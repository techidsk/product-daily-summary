# 数据库 —— 服务端直连 Postgres（Supabase），切勿暴露到前端
#
# 取串：Supabase 控制台 → Connect（或 Project Settings → Database）→
#       选 "Session pooler"（IPv4，端口 5432）→ 复制 URI → 填密码。
# ⚠️ 别用 "Direct connection"（db.<ref>.supabase.co）—— 它是 IPv6-only，
#    多数网络解析不到会连不上。
DATABASE_URL=postgresql://postgres.YOUR_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres

# 复制本文件为 .env 并填入真实值；.env 已被 .gitignore 忽略。
# 未配置时应用仍可运行（回退为实时抓取 + 内存缓存，无持久化/历史）。
