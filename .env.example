# ── 前端直读（浏览器，可公开）──────────────────────────────────
# Vite 会把 VITE_ 变量注入前端。publishable/anon key 可公开。
# Supabase 控制台 → Project Settings → API Keys
VITE_SUPABASE_URL=https://YOUR_REF.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxx

# ── 入库（服务端：本地 / GitHub Actions）切勿暴露到前端 ──────────
# Supabase → Connect → Session pooler（IPv4/5432；别用 IPv6-only 的 Direct）
DATABASE_URL=postgresql://postgres.YOUR_REF:YOUR_PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres

# ── 历史回填代理（仅 `npm run backfill` 用）──────────────────────
# archive.org / web.archive.org 在部分网络被封。若你本地有代理（如 Clash 的
# 127.0.0.1:7897），设置下面任一变量，回填脚本会自动把 archive.org 流量走代理。
# 不设则直连；GitHub Actions 上无需配置。
# HTTPS_PROXY=http://127.0.0.1:7897

# ── 广告（Google AdSense，可选）────────────────────────────────
# 不填 = 前端完全不渲染广告、不加载脚本，右侧广告位自动收起（默认隐藏）。
# AdSense 审核通过后，填入发布商 ID（ca-pub- 开头）即可上线，无需改任何代码。
# VITE_ADSENSE_CLIENT=ca-pub-XXXXXXXXXXXXXXXX
#
# 各广告位的 data-ad-slot 数字 ID（AdSense → 广告 → 按广告单元，每个单元一个数字）：
# VITE_ADSENSE_SLOT_RAIL_HALFPAGE=1234567890   # 右栏 300×600 半页
# VITE_ADSENSE_SLOT_RAIL_RECTANGLE=1234567891  # 右栏 300×250 中矩形
# VITE_ADSENSE_SLOT_INFEED_MOBILE=1234567892   # 移动端信息流 320×100
#
# 仅用于布局调试：即使没填 client 也显示虚线占位框；生产请留空。
# VITE_ADS_PLACEHOLDER=1

# 复制为 .env 填真实值；.env 已 gitignore。
# 部署：VITE_* 配到 Cloudflare Pages 环境变量；DATABASE_URL 配到 GitHub Actions secret。
